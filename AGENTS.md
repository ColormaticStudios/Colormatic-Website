This project is the official Colormatic Website. It is a static SvelteKit site with Typescript, Tailwind, and SASS.

Always format after editing a file: `bun x prettier -w <file>`.

Use `bun run check` and `bun run lint` to check the project after script changes.

Make use of existing systems where reasonable instead of writing code that doesn't fit in this project.
